#include <stdio.h>

const int n = 3

float average(int length, int array[]);

int main(void)
{
    int score[n]
    for(int i = 0; i < n; i++)
    {
        scores[i] = 2;
    }
    printf("%f\n", average(scores));
}
float average(int array[])
{
    int sum = 0;
    for (int value = 0; value < n; value++)
    {
        sum +=  array[value];
    }
    return sum/(float)n;
}